syntax = "proto3";

package test;

import "optype.proto";

// TestRequest has the required option, so we should not expect it to cause
// a failure
message TestRequest {
  option (praefect.op_type).op = ACCESSOR;
}
